@{
    ViewBag.Title = "Potwierdzenie";
}

@if (Request.IsAuthenticated && User.IsInRole("Supervisor") && User.IsInRole("Admin"))
{
    <p class="under-menu"><br /></p>
}

<hgroup class="title">
    <h3>@ViewBag.Title.</h3>
</hgroup>
<div>
    <p>
        Aby zresetować hasło, sprawdź swoją pocztę.
    </p>
</div>
	
